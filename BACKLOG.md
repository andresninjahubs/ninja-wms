# Backlog del proyecto — WMS 3PL Ninja Hubs

Estado del walking skeleton y pendientes priorizados. Actualizado al avanzar cada fase.

## ✅ Construido y probado (222 tests)

- **Dashboard AI · Fase A (v101)** — una sección nueva donde el administrador arma **su propio tablero conversando**: pide lo que quiere ver en sus palabras y el tablero se construye, con datos **en vivo que se refrescan solos cada 30 s** (el refresco pide datos, no llama al modelo). La decisión de fondo: **el LLM no escribe HTML ni código**, escribe una **especificación declarativa de widgets** (`domain/ai-dashboard.ts`) que el servidor valida widget por widget y el panel dibuja con sus propios componentes. Eso compra tres cosas: nada de lo que diga el modelo se ejecuta en el navegador; el tablero se redibuja siempre con datos frescos porque guarda la PREGUNTA y no la respuesta; y como es estructura, se puede editar **a mano** sin pasar por el modelo. Las fuentes de datos son las **44 herramientas de lectura** del copiloto, ejecutadas con el tenant y los permisos de quien mira: un widget no puede pedir nada que esa persona no pudiera ver por su cuenta, ni escribir nada. Lo que el modelo invente (una herramienta que no existe, un tipo de widget inventado, una agregación rara) se **rechaza con su motivo** y ese motivo se le muestra a la persona. Incluye: seis tipos de widget (KPI, tabla, barras, líneas, lista, texto), filtros, agrupación, orden y límite; **grilla de 12 columnas con arrastrar y redimensionar**; acomodo automático en el primer hueco libre; varios tableros por persona con crear, renombrar, duplicar y eliminar; editor manual de cada widget; un «¿Qué puedo pedir?» que lista **qué puede y qué no puede** hacer, generado del mismo catálogo (así nunca queda desactualizado); y «¿de dónde sale este dato?» por widget. Permiso nuevo `dashboard:ai`: solo **PLATFORM_ADMIN y ADMIN** (supervisor y operario reciben 403). Sin IA conectada, la sección igual funciona a mano. Probado de punta a punta contra un LLM de prueba: construyó los widgets pedidos y el sistema rechazó la herramienta inventada. Tests: 222.
- **Panel de control consolidado, según el diseño del cliente (v100)** — el dashboard era **por cliente**; ahora mira **toda la operación**, con el cliente como filtro (conmutador «Toda la operación / Cliente actual»). Se arma con **una sola llamada** (`GET /operations/:id/dashboard`) que devuelve todo lo que la pantalla muestra, y **se refresca sola cada 30 s** mientras el Dashboard está a la vista. Bloques: actividad operativa con comparativo y selector de período (24 h / 7 / 30 / 90 / personalizado); productividad de bodega; pre-facturación acumulada del mes por cliente; cinco tarjetas de control (órdenes por despachar **a tiempo** y **atrasadas** —posible gracias al deadline de la v99—, **precisión de preparación**, tiempo de preparación **B2B/B2C** y ocupación de bodega); carga pendiente por cliente; cola de picking por courier; órdenes por estado; reposición de embalaje contra el **stock mínimo**; y excepciones abiertas. Lo que el sistema todavía no mide no se inventa: viene en `faltantes` y la pantalla lo dice. Tests: 219.
- **Precisión de preparación: verificación al empacar (v100)** — el dato no existía (lo que había era exactitud de INVENTARIO, de los conteos). Ahora al empacar se puede **contar el bulto**: el sistema compara contra lo que la orden pedía y guarda el resultado en la orden (`packing.verification`) con las diferencias línea por línea. No bloquea el empaque —la mercadería igual sale, con la diferencia registrada— y el evento de auditoría la deja escrita. De ahí sale la precisión del panel: pedidos verificados, pedidos con diferencia y **qué porcentaje de los empaques se está verificando** (sin esa cobertura, el % sería una ilusión). Nuevo: `minStock` por insumo de embalaje, para que la reposición compare contra un mínimo de verdad y no solo contra el consumo.
- **Portada del login, sidebar colapsable y renombres (v100)** — la pantalla de entrada pasa a ser una portada: la caja de envío **se arma sola** (solapas plegándose, costuras dibujándose) y queda cerrada con la invitación a tocarla; al hacer clic **se abre y de adentro sale el formulario**, y recién ahí se escriben solos el nombre **Ninja WMS** y el eslogan **AI Native WMS**. Funciona con teclado, respeta `prefers-reduced-motion`, se abre sola a los 12 s para que nadie quede afuera, y está pensada tanto para escritorio como para teléfono. Además: el **menú lateral se colapsa a solo iconos** (la preferencia se recuerda en el navegador), el botón **Salir se movió al final del menú** —donde se busca— y se renombraron dos secciones: **Mensajes → Canal clientes** y **Canal de voz → Canal operaciones**.
- **Deadline de preparación + sandbox del agente (v99)** — el sistema sabía **cuánto llevaba esperando** una orden (horas detenida), no **para cuándo tenía que estar lista**. Ahora cada orden tiene un `dueAt`: lo trae el canal, o sale de la **hora de corte del courier** configurada en la operación, o del **SLA en horas** del cliente, o no existe (y entonces todo funciona como antes). De ahí cuelga el resto: la **cola de preparación** pone adelante lo vencido y lo que está por vencer, ordenado por holgura, y solo después aplica la prioridad de courier y el FIFO; la **bandeja del operario** prioriza por holgura contra el compromiso en vez de por tiempo transcurrido (el viejo impulso de "empacada hace +6 h" queda como respaldo para órdenes sin deadline); y el agente tiene una regla nueva, **`deadline_riesgo`**, que avisa ANTES de incumplir, más las herramientas `ordenes_por_vencer` (lectura) y `fijar_deadline_orden` (acción reversible, nivel 2). El deadline se puede fijar a mano desde el detalle de la orden **en cualquier estado abierto** —que el courier mueva su retiro no cambia las líneas ni el stock— y queda auditado. En el panel: columna Deadline en Órdenes con chip por urgencia (vencida / ≤1 h / en riesgo / holgada), campo opcional en el formulario, editor de cortes de courier en la Cola de preparación y campo de SLA en el mantenedor de clientes. Esquema: `SalesOrder.dueAt/dueSource`, `Seller.slaHoras`, `Operation.deadlineConfig` — todas columnas nuevas y nulables (`prisma db push` no borra nada). De paso se corrigió que **RESTOCK no tenía peso por tipo** en el recálculo de prioridades y caía después del re-slot. Tests: 217.
- **Sandbox de demostración del agente (v99)** — botón para el administrador de la operación (Clientes → «Sandbox de demostración»). Crea un **cliente de juguete nuevo por corrida** —nunca toca a los clientes reales— con 5 productos, 10 ubicaciones propias, stock repartido entre almacenaje, picking y dock, y **20 órdenes en todos los estados** con deadlines vencidos, críticos, en riesgo, holgados y sin compromiso. Deja servido todo lo que el agente sabe hacer: tareas pendientes de picking, empaque, despacho, recepción, guardado, reposición y conteo; tres operarios (una cargada, uno ocioso y una inactiva con tareas abiertas) para balancear, reasignar y liberar; órdenes listas para avanzar de estado; órdenes sin deadline para fijarlo; una orden sin stock suficiente; un lote que vence en 5 días; y órdenes estancadas y empacadas hace horas para las reglas de tiempo. Al terminar muestra el inventario de lo sembrado. Verificado contra el servidor: el agente levantó 6 alertas de deadline y ejecutó `fijar_deadline_orden`, `avanzar_estado_orden` y `balancear_carga` sobre estos datos.
- **U1 + U2 + S1 · Ubicaciones pegables, vista de lista y Facturación fuera del supervisor (v98)** — tres cosas del segundo round de feedback de Pamela. **U1:** al mover stock (Almacenado → «Guardar / mover») los tres desplegables pasaron a ser campos donde se **pega o se escribe**: el producto se resuelve por SKU, nombre o código de barras, y las ubicaciones por código sin importar mayúsculas ni espacios. El campo de origen solo acepta ubicaciones **que tengan stock de ese producto** y su lista muestra cuántas unidades hay en cada una; si hay una sola, se rellena sola, y si hay varias, el campo queda vacío con el aviso «en N ubicaciones con stock». El destino viene con la **sugerencia del motor de guardado** y solo acepta almacenaje o picking, nunca la misma ubicación de origen. Cada error se explica en su campo (no existe, sin stock, zona no válida, misma ubicación) y el botón queda bloqueado hasta que los tres campos son válidos. **U2:** Ubicaciones abre por defecto en **vista de lista** —una fila por ubicación con código, zona, ocupación, uso y estado— con **buscador por código** y **filtro por zona**, contador de resultados y exportación a Excel; el conmutador Lista/Tarjetas recuerda la preferencia en el navegador. **S1:** **Facturación salió de la vista del supervisor**: el permiso `billing:manage` ya no está en su rol, así que no ve el módulo en el menú y la API le responde 403 en tarifario, vista previa y emisión de facturas; el supervisor corre el piso, no la plata. El administrador de la operación la conserva completa; el cliente sigue viendo sus propias facturas. Tests: 212.

- **Reposición: la devolución a ubicación como tarea asignable (v97)** — corrige lo que la v96 dejaba a medias. Al cancelar, la mercadería ya recolectada **no vuelve sola a su estante**: está en un carro, y darla por disponible dejaba que otra orden la reservara sin que estuviera ahí. Ahora aterriza en una ubicación de **reposición** por operación (`DEV-REPOSICION`, en zona de recepción: el stock se ve y se cuenta, pero no es reservable) y queda pendiente una **tarea de tipo RESTOCK**, con todo lo que eso implica: aparece en el pool, se asigna desde Asignaciones, entra al balanceo automático, se prioriza en la cola del operario (entre guardado y conteo) y se ejecuta desde la app con el flujo de guardado —origen preseleccionado y **destino sugerido** (la ubicación de la que salió) pero cambiable. Al completarla, el stock vuelve al estante y la tarea se cierra. En el panel vive como pestaña **«Por reponer»** dentro de Almacenado, junto a «Desde recepción», cada una con su contador. Sin cambios de esquema (los tipos de tarea son texto). Tests: 212.
- **O7 · Cancelar en cualquier estado devolviendo el stock a su ubicación (v96)** — antes solo se podía cancelar antes de pickear. Ahora se cancela en **ingresada, reservada, en picking, pickeada y empacada**, y el stock vuelve a la bodega con la distinción correcta: lo que seguía **reservado** vuelve a disponible, y lo que ya fue **recolectado** se devuelve como movimiento de devolución a la **misma ubicación de la que salió** (la que quedó registrada en cada reserva), sin inventar stock ni descuadrar el kardex. La confirmación calcula y muestra antes de cancelar cuántas unidades se liberan, cuántas se devuelven y a qué ubicaciones, y avisa que alguien debe reponerlas físicamente; en una orden empacada avisa además que los insumos de embalaje usados no se reponen. Cancelar una orden asignada libera la tarea del operario y la saca del ledger. **Despachada** sigue sin poder cancelarse: la mercadería salió de la bodega y el camino es una devolución, que registra en qué estado vuelve. **Eliminar quedó bloqueado en todo nivel**: el método rechaza cualquier llamada salvo la consolidación interna de filas duplicadas creadas por reintentos del webhook. Tests: 211.
- **O4b · Las recepciones de origen en el aviso de falta de stock (v95)** — cuando el faltante está en el dock de recepción, la ventana ya no dice solo cuántas unidades hay: lista **de qué recepciones vienen y cuánto de cada una**, con su n° de documento, proveedor y fecha, para ir a buscarlas sin revisar las recepciones una por una. Como el guardado no se registra contra una recepción concreta, la atribución es FIFO —lo ya guardado se descuenta de las más antiguas—, de modo que la suma de lo pendiente siempre cuadra con el saldo real del dock. Tests: 207.
- **O4 · Falta de stock: aviso por orden con todos los faltantes (v94)** — el motivo dejó de ser una notificación pasajera con el texto crudo del motor de inventario. Ahora la reserva revisa **todas** las líneas antes de comprometer nada y abre una ventana centrada, por orden, con una fila por producto faltante: SKU, nombre, cuánto pide, cuánto hay disponible, cuánto falta y **dónde está el resto** — en particular cuántas unidades están en el dock de recepción sin guardar, que es el caso más frecuente y se resuelve con un guardado, no con una compra; en ese caso la ventana ofrece ir directo a Almacenado. Antes la reserva se detenía en el primer producto que fallaba y había que arreglar y reintentar tantas veces como productos faltaran. La reserva sigue siendo todo o nada. El mismo detalle se reutiliza en la **reserva masiva** (cada orden fallida abre su propio detalle) y al **editar una orden reservada** (v93). Los errores del dominio pueden llevar datos estructurados (`DomainError.data`) que la API expone en `data`. Tests: 206.
- **O3 · Editar una orden ya creada, incluso reservada (v93)** — antes solo se podía editar mientras estaba *Ingresada*. Ahora también se edita una orden **Reservada**: como hay stock comprometido, guardar es una operación en tres pasos —liberar las reservas, aplicar los cambios y volver a reservar— y es **todo o nada**: si el stock no alcanza para las líneas nuevas, se restauran las líneas anteriores con su reserva y la orden queda exactamente como estaba, con un mensaje que dice qué faltó. Se pueden cambiar todos los campos y agregar o quitar productos; el **N° de orden queda bloqueado** porque es la referencia del pedido para el cliente y el OMS. Desde *En picking* en adelante sigue bloqueada, con un mensaje que explica por qué (la mercadería ya salió de su ubicación). Si la orden estaba asignada a un operario, se actualiza su estimación de trabajo. El evento de auditoría registra el antes → después. Tests: 204.
- **O2 · Detalle de orden horizontal, con el nombre del producto (v92)** — el detalle dejó de ser el panel lateral angosto y pasa a ser una ventana centrada de **ancho fijo** (1160 px, el mismo para todas las órdenes, sin depender del contenido): a la izquierda los datos de la orden, el empaque y las etiquetas; a la derecha las líneas y, bajo ellas, tareas e historial en dos columnas. Cada columna hace su propio scroll, así la ventana tampoco cambia de alto de una orden a otra. En cada línea el **nombre del producto va debajo del SKU**, con el lote en una tercera línea cuando existe, y el encabezado resume productos y unidades. En el teléfono las columnas se apilan.
- **O1 · Buscar el producto por SKU o por nombre (v91)** — el autocompletado dejó de ser el `<datalist>` nativo (que solo busca por el código) y pasó a ser una lista propia que filtra por **SKU, nombre del producto y código de barras** a la vez, resaltando en verde la parte que coincide. Se recorre con las flechas, se elige con Enter o clic, y se cierra con Esc; en el teléfono se abre hacia arriba si no hay espacio abajo y apila código y nombre en dos líneas. Escribir el nombre completo también resuelve al SKU al salir del campo. El campo ya no se marca en rojo mientras se escribe: solo al salir o al guardar.
- **O1 · SKU pegable al crear/editar una orden (v90)** — el producto dejó de ser un desplegable: ahora es un campo donde se pega o se escribe el código. Acepta el SKU o el código de barras, ignora mayúsculas y espacios, y normaliza al SKU real del catálogo. Bajo el campo aparece el nombre del producto para confirmar que es el correcto, y si el código no existe el campo se marca en rojo con el aviso y el guardado se bloquea nombrando el código exacto. Además, pegar una COLUMNA de códigos (desde Excel) crea una línea por código de una vez. Mantiene el autocompletado con todo el catálogo.
- **Logo del sidebar visible siempre (v89)** — el logo no aparecía en el panel de escritorio: al montar el menú móvil (v81) la marca se envolvía en una cabecera `.m-side-head` que se ocultaba fuera de móvil, llevándose el logo con ella; ahora la cabecera está siempre visible y lo único exclusivo de móvil es el botón de cerrar el cajón. Además el wordmark recupera su color: `fill="var(--primary)"` en un `<tspan>` no se resuelve (salía negro), así que el color va por CSS. Y en white-label, si el logo del tenant no carga (404 o ruta mala) se conserva el wordmark en vez de dejar el sidebar vacío.
- **Contador de órdenes por estado (v87)** — los chips de filtro de Órdenes muestran cuántas órdenes hay en cada etapa (Todas, Ingresada, Reservada, En picking, Pickeada, Empacada, Despachada, Cancelada), para leer la carga de trabajo de un vistazo sin abrir cada filtro; se recalculan en cada refresco de datos y los estados vacíos quedan atenuados.
- **Cierre de tareas desde la app del operario (v86)** — al terminar la tarea en curso, ésta sale de «Mis tareas» de inmediato. Causa: el picking dirigido (`pick-task`, el que usa la app) no cerraba la asignación PICK ni el ledger al dejar la orden PICKED (solo lo hacía el picking global `confirmPick`); ahora ambos caminos cierran la asignación, marcan la tarea PICK como hecha y abren PACK. La app además refresca la bandeja apenas termina (no solo al volver a inicio), al volver al frente el teléfono, y muestra «Volver a mis tareas» cuando la tarea concluyó. **Recepción por tarea**: el cotejo se registra contra la orden de recepción (solo se escanea el producto y la cantidad; el stock aterriza en la ubicación de recepción de la orden), con avance por línea y botón «Cerrar recepción con faltante» (`closeReceipt` ahora también cierra la tarea RECEIVE). Guardado/re-slot cierran la tarea al primer movimiento. Tests: 200.
- **App del operario rediseñada: bandeja de tareas primero (v85)** — la PWA abre en «Mis tareas»: tarjeta grande «▶ Siguiente» (o «En curso») y lista numerada en orden de ejecución con tipo, referencia, cliente, unidades y el motivo de la prioridad; se refresca sola cada 30 s. Pestaña «Disponibles» (solo si el administrador lo permite: nuevo ajuste por operación `operatorSelfPickup`, botón «🧑‍🏭 Tomar tareas» en Asignaciones) con las tareas sin asignar de la bodega en orden de prioridad y botón «Tomar». Pestaña «Operar libre» con los flujos de siempre. Al **Iniciar** una tarea se marca en curso (`POST /assignments/start`) y se abre el flujo con contexto: picking muestra la lista de la orden (SKU, ubicación, avance) y registra cada escaneo contra la orden (`pick-task`, la orden pasa a PICKED al completar); guardado/re-slot preseleccionan el origen; recepción abre el escaneo de ingreso; **empaque y despacho** se cierran desde la app con una pantalla rápida (bultos / courier + tracking). Nuevos endpoints `GET /assignments/board`, `POST /assignments/take`, `POST /assignments/start`, `GET/PUT /assignments/self-pickup`. Cliente en el login pasa a ser opcional (solo para operar libre). Service worker: solo cachea el shell de la app, nunca la API. Esquema aditivo: `Operation.operatorSelfPickup`.
- **Bandeja del operario con orden de ejecución (v84, adelanto de Fase 3)** — cada asignación abierta recibe una `priority` calculada por el sistema: peso del tipo (despacho < empaque < picking < recepción < guardado < conteo < re-slot) × 1000 + posición en su cola (picking por courier+FIFO, guardado por llegada, recepciones parciales primero, conteos por plan), con impulsos por orden de prioridad alta, pedido empacado hace +6 h (SLA) y **instrucciones vigentes del administrador** ("hoy priorizar Chilexpress" / un cliente). Se recalcula en cada ciclo del agente, al asignar y al leer la bandeja (≤ 1 vez/min). `GET /assignments/mine` devuelve la lista en orden de ejecución con `position`, `next` y `priorityReason`; la PWA muestra la bandeja numerada con «▶ Siguiente» y el motivo; el panel de Asignaciones por operario muestra # y «por qué en este orden». Esquema aditivo: `WorkAssignment.priority`, `priorityReason`.
- **Agente autónomo · Fase 1 (v83)** — (1) **Reloj propio en el servidor** (`src/infra/agent-scheduler.ts`): ciclo cada `AGENT_INTERVAL_SEC` con lock por operación; el panel ya no dispara barridos; rollup diario de analítica incluido. (2) **Alertas por entidad**: cada regla genera una alerta por orden/SKU/lote/operario con su referencia (`entityRef`, `entityType`), dedupe y cooldown por entidad. (3) **Contexto en capas** para el LLM: perfil del tenant (cacheado 5 min, con reglas de negocio), estado operativo compacto con listas accionables y deltas, memoria del agente (instrucciones vigentes + diario) y detalle recortado por prioridad; secciones fallidas se declaran, nunca desaparecen. Pasó de ~60 KB truncados a ciegas a ~8 KB priorizados. (4) **Política de autonomía** (`src/domain/agent-policy.ts`): nivel 0–3 por operación, nivel mínimo por acción, modo sombra, pausa, límites por ciclo y por hora; el modo "confirmación" protege ahora TODAS las escrituras de nivel ≥2 con propuestas genéricas `{tool,args}` confirmables desde chat y voz; sin identidad no hay escritura en producción. (5) **Modo sombra**: el agente registra "habría ejecutado" (`AgentAction` agent `agent-shadow`) sin tocar nada. (6) **Notificaciones** por correo y webhook de alertas críticas/propuestas por ciclo. (7) **Diario e instrucciones** (`AgentJournal`): memoria persistente; instrucciones desde el panel o con la herramienta `guardar_instruccion`. (8) **Planificación LLM opcional y presupuestada** (cada N min, máximo de llamadas/día, solo si hay algo accionable). (9) Loop LLM endurecido: 14 rondas, presupuesto de 30 herramientas y 90 s por turno, reintento ante 429/5xx, paridad entre proveedores, auditoría de lecturas. Esquema: columnas aditivas en `CopilotSetting`, `AgentAlert.entityType`, tabla nueva `AgentJournal` (`db push` sin pérdida). Panel: página "Agente" con estado, ajustes, instrucciones y diario.
- **Costos de insumos de embalaje (v82)** — cada reposición registra el **costo unitario de compra** (más referencia y usuario). El sistema lleva el **costo promedio ponderado (PMP)** por insumo (recalculado en cada ingreso; las salidas no lo alteran), el costo de la última compra, el margen contra el precio de cobro y el valor del stock. Los consumos al empacar y los ajustes quedan **valorizados al PMP del momento**, y Rentabilidad usa ese costo real en vez del % estimado cuando existe. Esquema: columna ADITIVA `PackagingMovement.unitCost Int?` (`db push` sin pérdida).
- **Fix producción: «Error interno» al crear / recepcionar una orden de recepción (v82)** — en Prisma, la tabla normalizada `ReceiptLine` se llenaba con `l.qty` (campo del payload) en vez de `expectedQty` (campo del dominio), así que la cantidad llegaba vacía y Postgres rechazaba el insert; como la cabecera ya se había guardado, la recepción aparecía al refrescar. Ahora se mapea `expectedQty`, se ignoran vencimientos inválidos y cabecera + líneas van en una sola transacción. Sin cambio de esquema.
- **Feedback cliente, lote 1 (v82)** — (C1) **carga masiva de ubicaciones** por Excel/CSV (`/operations/:id/location-import` template/export/preview/commit, mismo patrón que productos: el código es la clave, nuevas se crean, existentes se editan con diff y confirmación; zonas en español o inglés). (C2) **eliminar ubicación** (`DELETE /locations/:id`) solo si nunca tuvo movimientos ni recepciones abiertas; si tuvo historia responde con mensaje claro y el panel ofrece desactivarla. (C3) **reposición de insumos de embalaje** con referencia (proveedor / guía / factura) y usuario, botón «Historial» por insumo con saldo, reposiciones, consumos y ajustes. (B5) botón «Historial» global de Productos eliminado (queda el de cada fila). (B3) modal «Nueva orden» a ancho completo con cabecera de columnas y campo Cantidad siempre visible (también en móvil). Sin cambios de esquema de base de datos.
- **Fix producción: historial de órdenes no se persistía en Prisma (v81)** — el modelo `SalesOrder` no tenía columna `events` (solo ReturnOrder/ReceiptOrder), así que en producción toda orden volvía sin historial y la factura contaba 0 despachos. Se agregó la columna `events Json @default("[]")` (cambio de esquema ADITIVO, `db push` la crea sin pérdida) y el repositorio la persiste (y `shipTo` en update); facturación usa `shipment.shippedAt` como respaldo para órdenes despachadas guardadas antes del fix.
- **Cambio de estado masivo de órdenes (v81)** — admin/supervisor/plataforma: casillas por fila + «todas las visibles» del filtro, barra flotante con las transiciones válidas para la selección (reservar, a picking, confirmar picking completo, empacar 1 bulto, despachar con courier opcional, cancelar, reactivar); se aplica orden por orden con resumen de ok/omitidas/errores; selección persistente entre refrescos; también en móvil.
- **Vista en vivo por sondeo (v81)** — Dashboard, Todos los clientes, inventario, recepción, almacenado, movimientos, etc. se recargan solos (20–60 s según sección) mientras la pestaña está visible; se pausa con formularios/paneles abiertos o tutorial corriendo; indicador «al día / hace N s» junto al título con refresco manual. Órdenes/cola siguen a 6 s.
- **Facturación: regla de corte por despacho (v81)** — despacho, picking (unidades PICK ligadas a la orden, con respaldo en las líneas) y embalaje se cobran por las órdenes cuyo evento DESPACHADA cae en el período; picking/empaque de meses anteriores se factura junto con el despacho, y lo no despachado no se cobra. Test de corte entre meses.
- **Panel responsivo para móviles (v81)** — `webadmin/mobile.css` + `mobile.js`: menú en cajón deslizante (☰), barra superior compacta con botón de contexto (buscador, operación, cliente, sesión), tablas como tarjetas apiladas, paneles/modales a pantalla completa, controles táctiles; sin cambios en escritorio. Probado en iPhone, Android e iPad (vertical y horizontal).
- **Módulos ocultos por versión (v81)** — `GET /ui-config` + env `HIDDEN_MODULES` (default: voz, costos, plan, aiaudit, asignaciones, agente): se quitan del menú, de la navegación directa y del Centro de aprendizaje; el super-admin de plataforma los sigue viendo.
- **Tutoriales guiados por sección (v81)** — tour interactivo con narración por voz sobre la interfaz real (foco por elemento, auto-avance, pausa, atajos), auto-ofrecido la primera vez y siempre disponible desde «▶ Tutorial» y el Centro de aprendizaje; más videos MP4 grabados automáticamente (`scripts/record-tutorials.js`). Ver `TUTORIALES.md`.
- **Multi-operación (multi-administrador)** — `Operation` como tenant de más alto nivel: cada operación tiene sus propias bodegas, ubicaciones, sellers, usuarios e inventario, aisladas entre sí. Rol `PLATFORM_ADMIN` (Ninja Hubs) por encima que crea operaciones y ve todas. Enforcement: un admin de una operación no puede ver ni actuar sobre otra; invariante de que una ubicación pertenece a la misma operación del seller. Dos niveles de aislamiento: operación → seller.
- **Códigos de barra y unidades de medida** — packs multi-nivel por SKU (unidad EAN, caja DUN, pallet…), cada uno con su código y factor a la unidad base; resolución automática del nivel escaneado; **escaneo en recepción, guardado y picking** (producto + bin) con conversión a múltiplos del EAN. El inventario se lleva en unidades base.
- **Usuarios, roles y permisos** — mantenedor de usuarios (CRUD), roles ADMIN / SUPERVISOR / OPERATOR / CLIENT, permisos por acción, guard en la API, y **auditoría**: cada movimiento del ledger queda firmado por el usuario que lo hizo (`actor`). Usuarios CLIENT aislados a su propio seller.

- **Núcleo multi-cliente (3PL)** — aislamiento total por `sellerId`; SKU scoped al seller (mismo código no colisiona); ubicaciones compartidas.
- **Inventario como ledger inmutable** — stock = suma de movimientos; nunca negativo; estados (disponible / reservado / cuarentena / dañado / en tránsito).
- **Recepción y guardado** con trazabilidad por lote.
- **Guardado caótico dirigido** — sugerencia de ubicación por rotación ABC, capacidad y consolidación.
- **Órdenes desde el OMS** — contrato canónico (seller + canal), multi-SKU, lote opcional, B2C/B2B.
- **Reserva (allocation)** — disponible = físico − reservado; anti-sobreventa; full-or-nothing con rollback.
- **Picking y despacho** — pick list, confirmación de picking, despacho paquetería (B2C) / transporte (B2B).
- **Estrategia de picking configurable por seller** — FIFO / FEFO / lote-serie dirigido.
- **Conteo cíclico configurable por seller** — planificador ABC / ubicación / aleatorio + ejecución con reconciliación (ajustes al ledger) y reporte de varianzas.

## 🔜 Pendiente (priorizado)

### UI (pendiente de iterar)
- [ ] **Panel, consola y PWA con conmutador de operación** — hoy las interfaces muestran una sola operación; falta reflejar el selector de operación (para PLATFORM_ADMIN) y acotar la vista de cada administrador a la suya.

### P1 — Control y seguridad
- [x] **Perfiles de usuario, roles y mantenedor de usuarios** — hecho.
- [x] **Autorización** — permisos por acción + guard; el usuario autenticado es el `actor` del ledger — hecho.
- [ ] **Autenticación real** — hoy el token es el id/email del usuario (skeleton). Falta login con contraseña (hash bcrypt/argon2) y **JWT firmado** con expiración/refresh, sobre HTTPS. Cambio acotado: solo toca login + guard, no el dominio.
  - Decisión pendiente con Andrés: **auth propia** (login+JWT dentro del WMS) vs. **proveedor de identidad / SSO** (Google/Microsoft, Auth0/Cognito/Keycloak/Clerk). Probable: SSO para staff del operador + cuentas propias para clientes. Extras: 2FA, bloqueo por intentos, reset de contraseña.
- [ ] **Bitácora de auditoría no-stock** — registrar logins, cambios de configuración y de usuarios (el ledger ya audita los movimientos de stock).
- [ ] **RLS en PostgreSQL** — reforzar el aislamiento por seller a nivel de base de datos.

### P2 — Integraciones reales (requieren credenciales / ambientes del lado de Ninja Hubs)
- [ ] **OMS** — webhooks de estado de fulfillment; adopción del contrato de órdenes.
- [ ] **ERP** (Bsale / Defontana / Odoo) — sincronización de productos, compras y valorización.
- [ ] **Courier / transporte** (Chilexpress / Starken / Blue Express) — cotización, guía y tracking.

### P3 — Operación avanzada
- [ ] **Conteo por evento (opportunity counting)** — disparar conteo automático al llegar a cero/negativo o ante descuadres en picking.
- [ ] **Reposición automática** de ubicaciones de picking desde reserva cuando bajan del mínimo.
- [ ] **Rastreo serial** unidad-por-unidad (hoy la trazabilidad es por lote).
- [ ] **Ajustes con aprobación** — flujo de autorización para varianzas de conteo sobre un umbral.

### P4 — Cliente y facturación 3PL
- [ ] **Portal / reportería por seller** — cada cliente ve sus KPIs (exactitud, líneas/hora, ocupación, dock-to-stock).
- [ ] **Base de facturación 3PL** — cargos por almacenaje, picking y despacho por seller.
- [ ] **B2B avanzado** — ASN al destino, cita de entrega (campos ya previstos en el contrato de órdenes).

### P5 — Operación en piso
- [x] **PWA operador con lector de código** — app instalable (`pwa/`) con cámara vía `BarcodeDetector` (+ ingreso manual), login, recepción/guardado/picking/consulta y conversión de unidades. Pendiente de endurecer: pruebas en dispositivos reales, íconos PNG maskable, modo offline de operaciones (cola de reintento), y lector con librería propia para navegadores sin `BarcodeDetector` (ej. iOS Safari).
- [ ] **Endurecimiento** — rendimiento, observabilidad, backups del ledger, despliegue productivo.
